import { DurationPipeTsPipe } from './duration.pipe.ts.pipe';

describe('DurationPipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationPipeTsPipe();
    expect(pipe).toBeTruthy();
  });
});
